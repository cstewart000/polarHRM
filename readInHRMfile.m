
function [ret] = readInHRMfile(path)


	File file = fopen(path)

	fileData = textscan(fileId,%s)


	% Split the file around the "[HRData]" marker

	fileData

	%dataString = strspilt(fileData, '\n');

	%hrDataValues = strspilt(dataString(2),'\n'))

end